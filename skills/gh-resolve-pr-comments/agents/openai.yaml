interface:
  display_name: "GH Resolve PR Comments"
  short_description: "Resolve PR comments in isolated worktrees"
  default_prompt: "Use $gh-resolve-pr-comments to create an isolated worktree for this PR, then classify and resolve its review comments."
